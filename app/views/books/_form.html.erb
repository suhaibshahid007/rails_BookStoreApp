<%= simple_form_for @book, :html => { :multipart => true} do |b| %>
	
	<%= select_tag(:category_id, options_for_select(@categories), :prompt => "Select a Category") %>
	<%=b.input :title %>
	<%=b.input :description %>
	<%=b.input :author %>
	<%=b.file_field :image %><br>
	<%=b.button :submit, :class => 'btn-custom2' %>
	
<% end %>
